# Memory Guardian Configuration
# 内存守护者配置文件

# Memory usage thresholds (percentage of physical RAM)
# 内存使用率阈值（物理内存百分比）
thresholds:
  notice: 70     # 🟡 Notice level / 注意级别
  warning: 80    # 🟠 Warning level / 警告级别
  critical: 90   # 🔴 Critical level / 严重级别

# Check interval in seconds (for daemon mode)
# 检查间隔（秒，守护模式使用）
check_interval: 30

# Cooldown between notifications (seconds)
# 通知冷却时间（秒）
notification_cooldown: 60

# Process types to monitor and clean
# 要监控和清理的进程类型
target_processes:
  - python
  - python3
  - python.exe
  - pythonw.exe
  - node
  - node.exe

# Enable desktop notifications
# 启用桌面通知
enable_notifications: true

# Enable console output
# 启用控制台输出
enable_console: true

# Language settings (bilingual by default)
# 语言设置（默认双语）
language: bilingual  # Options: en, zh, bilingual
